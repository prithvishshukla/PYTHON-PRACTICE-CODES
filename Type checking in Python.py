# Prompt the user to enter a value and store it as a string in the variable 'a'
a = input("Enter number a: ")

# Print the type of the variable 'a', which will be 'str' (string)
print("type(a)", type(a))
